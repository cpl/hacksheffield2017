'use strict';

describe('myApp.peopleSearch module', function() {

  beforeEach(module('myApp.peopleSearch'));

  describe('peopleSearch controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('View2Ctrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});