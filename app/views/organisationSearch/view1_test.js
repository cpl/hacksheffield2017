'use strict';

describe('myApp.organisationSearch module', function() {

  beforeEach(module('myApp.organisationSearch'));

  describe('organisationSearch controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('View1Ctrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});